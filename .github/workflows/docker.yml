# name: Docker

# # TODO(ci): os SHAs das actions abaixo foram escritos sem acesso ao registry. ANTES do
# # primeiro run, confirme cada digest com
# # `gh api repos/<owner>/<repo>/git/ref/tags/<tag> --jq .object.sha`.
# # A convenção do projeto é pinar por SHA (nada de @v4): tag é mutável e permite repin
# # silencioso — vetor clássico de supply chain.

# # Só DEPOIS do merge: imagem de código não mergeado não sobe para o registry.
# on:
#   push:
#     branches: [main, staging, develop]

# jobs:
#   image:
#     if: github.event_name == 'push'
#     runs-on: ubuntu-latest
#     permissions:
#       contents: read
#       packages: write
#     steps:
#       - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2

#       - uses: docker/setup-buildx-action@c47758b77c9736f4b2ef4073d4d51994fabfe349 # v3.7.1

#       - uses: docker/login-action@9780b0c442fbb1117ed29e0efdff1e18412f7567 # v3.3.0
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       # Tag por branch: main → :latest, staging → :staging, develop → :development
#       - name: Resolver a tag
#         id: tag
#         run: |
#           case "${{ github.ref_name }}" in
#             main)    echo "value=latest" >> "$GITHUB_OUTPUT" ;;
#             staging) echo "value=staging" >> "$GITHUB_OUTPUT" ;;
#             develop) echo "value=development" >> "$GITHUB_OUTPUT" ;;
#           esac

#       - uses: docker/build-push-action@4f58ea79222b3b9dc2c8bbdd6debcaf3b2c2f6a3 # v6.9.0
#         with:
#           context: .
#           push: true
#           tags: ghcr.io/${{ github.repository }}:${{ steps.tag.outputs.value }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
